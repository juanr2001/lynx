<div>
  <%= form_tag(links_path, method: :post) do %>

<div>
	<%= label_tag(:title, "Title: ") %>
	<%= text_field_tag(:title, nil, size:40) %>
</div>

<div>
	<%= label_tag(:url, "Url: ") %>
	<%= text_field_tag(:url, nil, size:40) %>
</div>

<%= submit_tag(@message) %>
<% end %>
</div>

